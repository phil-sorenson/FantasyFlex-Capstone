// Constant Button/Link/<a> on NavBar (top of page or side) that when opened shows a list of ALL leagues already imported & a list of platforms to log in to in the form of button/Links 
    // Platform buttons -- When clicked, a log in window will pop up {'Login.js' & 'LoginForm.jsx'}


const EditLeagues = (props) => {
    
    
    
    return ( 
        <div>

        </div>
     );
}
 
export default EditLeagues;